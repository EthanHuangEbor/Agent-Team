# Agent-Team 工作法

This repository is governed by the canonical `$sansheng-liubu` skill at `skills/sansheng-liubu/SKILL.md`.

For formal work:

1. Use the skill as the primary workflow.
2. Follow the order: 太子 -> 中书省 -> 门下省 -> 尚书省 -> 六部 -> 刑部/门下复核 -> 回奏.
3. Use real subagents when available; otherwise use labeled role passes.
4. Enforce the two-round self-iteration cap.
5. Do not introduce a separate state CLI as the required control plane.

Before final output, read `skills/sansheng-liubu/references/output-contract.md` and include evidence for actual work performed.
